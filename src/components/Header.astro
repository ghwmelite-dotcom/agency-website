---
import { SITE_CONFIG } from '@/config';
---

<header class="header">
  <nav class="nav container">
    <a href="/" class="logo">
      <img id="header-logo-img" alt="Logo" style="display: none; height: 40px; object-fit: contain;" />
      <span class="logo-text text-gradient" id="header-site-name">{SITE_CONFIG.name}</span>
    </a>

    <ul class="nav-links">
      <li class="nav-dropdown">
        <a href="/#services" class="dropdown-trigger">
          Services
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </a>
        <div class="dropdown-menu">
          <div class="dropdown-grid">
            <div class="dropdown-col">
              <h4>Web & Mobile</h4>
              <a href="/services/custom-web-application-development">Web Development</a>
              <a href="/services/mobile-app-development-services">Mobile Apps</a>
              <a href="/services/ios-app-development">iOS Development</a>
              <a href="/services/android-app-development">Android Development</a>
            </div>
            <div class="dropdown-col">
              <h4>Design & UX</h4>
              <a href="/services/ui-ux-design-services">UI/UX Design</a>
              <a href="/services/web-design-services">Web Design</a>
              <a href="/services/progressive-web-app-development">Progressive Web Apps</a>
            </div>
            <div class="dropdown-col">
              <h4>Enterprise</h4>
              <a href="/services/enterprise-software-development">Enterprise Software</a>
              <a href="/services/saas-application-development">SaaS Development</a>
              <a href="/services/api-development-services">API Development</a>
            </div>
            <div class="dropdown-col">
              <h4>More Services</h4>
              <a href="/services/ecommerce-development">E-Commerce</a>
              <a href="/services/cms-development">CMS Development</a>
              <a href="/services/mvp-development">MVP Development</a>
              <a href="/sitemap-pages" class="view-all">View All →</a>
            </div>
          </div>
        </div>
      </li>
      <li class="nav-dropdown">
        <a href="/sitemap-pages" class="dropdown-trigger">
          Solutions
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </a>
        <div class="dropdown-menu">
          <div class="dropdown-grid">
            <div class="dropdown-col">
              <h4>By Industry</h4>
              <a href="/industries/healthcare-software-development">Healthcare</a>
              <a href="/industries/fintech-software-development">Fintech</a>
              <a href="/industries/education-software-development">Education</a>
              <a href="/industries/elearning-platform-development">E-Learning</a>
            </div>
            <div class="dropdown-col">
              <h4>By Technology</h4>
              <a href="/technologies/react-development">React</a>
              <a href="/technologies/flutter-development">Flutter</a>
              <a href="/technologies/nextjs-development">Next.js</a>
              <a href="/technologies/python-development">Python</a>
            </div>
            <div class="dropdown-col">
              <h4>By Solution</h4>
              <a href="/solutions/startup-mvp-development">Startup MVP</a>
              <a href="/solutions/ai-powered-application-development">AI Development</a>
              <a href="/solutions/cloud-migration-services">Cloud Migration</a>
              <a href="/solutions/enterprise-digital-transformation">Digital Transformation</a>
            </div>
            <div class="dropdown-col">
              <h4>Explore</h4>
              <a href="/sitemap-pages" class="view-all">All Solutions →</a>
              <a href="/industries/retail-software-development">Retail</a>
              <a href="/technologies/nodejs-development">Node.js</a>
              <a href="/solutions/custom-crm-development">Custom CRM</a>
            </div>
          </div>
        </div>
      </li>
      <li><a href="/#portfolio">Portfolio</a></li>
      <li><a href="/blog">Blog</a></li>
      <li><a href="/booking">Book a Call</a></li>
    </ul>

    <div class="nav-actions">
      <button class="theme-toggle" data-theme-toggle aria-label="Toggle theme">
        <svg class="sun-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="5"/>
          <line x1="12" y1="1" x2="12" y2="3"/>
          <line x1="12" y1="21" x2="12" y2="23"/>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
          <line x1="1" y1="12" x2="3" y2="12"/>
          <line x1="21" y1="12" x2="23" y2="12"/>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
        <svg class="moon-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </button>

      <a href="/#contact" class="btn btn-primary">Get Started</a>

      <button class="mobile-menu-toggle" aria-label="Toggle menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--glass-border);
    box-shadow: var(--shadow-sm);
  }

  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: var(--header-height);
    gap: 2rem;
  }

  .logo {
    font-size: 1.5rem;
    font-weight: 800;
    font-family: var(--font-display);
    text-decoration: none;
  }

  .logo-text {
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-links a {
    color: var(--color-text);
    font-weight: 500;
    font-size: 0.95rem;
    transition: color var(--transition-fast);
    position: relative;
  }

  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--gradient-primary);
    transition: width var(--transition-base);
  }

  .nav-links a:hover {
    color: var(--color-primary);
  }

  .nav-links a:hover::after {
    width: 100%;
  }

  /* Dropdown Navigation */
  .nav-dropdown {
    position: relative;
  }

  .dropdown-trigger {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .dropdown-trigger svg {
    transition: transform var(--transition-fast);
  }

  .nav-dropdown:hover .dropdown-trigger svg {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    margin-top: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    min-width: 600px;
    padding: var(--spacing-lg);
    opacity: 0;
    visibility: hidden;
    transform: translateX(-50%) translateY(-10px);
    transition: all var(--transition-base);
    pointer-events: none;
  }

  .nav-dropdown:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
    pointer-events: all;
  }

  .dropdown-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--spacing-lg);
  }

  .dropdown-col h4 {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-muted);
    margin-bottom: var(--spacing-sm);
    font-weight: 600;
  }

  .dropdown-col a {
    display: block;
    padding: 0.5rem 0;
    color: var(--color-text);
    font-size: 0.9rem;
    transition: all var(--transition-fast);
  }

  .dropdown-col a::after {
    display: none;
  }

  .dropdown-col a:hover {
    color: var(--color-primary);
    padding-left: 0.5rem;
  }

  .dropdown-col a.view-all {
    color: var(--color-primary);
    font-weight: 600;
    margin-top: var(--spacing-sm);
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .theme-toggle {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-full);
    border: none;
    background: var(--color-surface);
    color: var(--color-text);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--transition-base);
    position: relative;
  }

  .theme-toggle:hover {
    background: var(--color-bg-alt);
    transform: scale(1.05);
  }

  .sun-icon,
  .moon-icon {
    position: absolute;
    transition: all var(--transition-base);
  }

  [data-theme="light"] .moon-icon {
    opacity: 0;
    transform: rotate(90deg);
  }

  [data-theme="dark"] .sun-icon {
    opacity: 0;
    transform: rotate(-90deg);
  }

  .mobile-menu-toggle {
    display: none;
    flex-direction: column;
    gap: 4px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
  }

  .mobile-menu-toggle span {
    width: 24px;
    height: 2px;
    background: var(--color-text);
    border-radius: 2px;
    transition: all var(--transition-base);
  }

  @media (max-width: 768px) {
    .header {
      padding: 0 var(--spacing-md);
    }

    .nav {
      height: 70px;
    }

    .logo {
      font-size: 1.25rem;
    }

    .nav-links {
      position: absolute;
      top: 70px;
      left: 0;
      right: 0;
      flex-direction: column;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      padding: 2rem var(--spacing-md);
      gap: 1.5rem;
      border-bottom: 1px solid var(--glass-border);
      transform: translateY(-100%);
      opacity: 0;
      pointer-events: none;
      transition: all var(--transition-base);
      max-height: calc(100vh - 70px);
      overflow-y: auto;
    }

    .nav-links.active {
      transform: translateY(0);
      opacity: 1;
      pointer-events: all;
    }

    /* Mobile dropdown styles */
    .dropdown-menu {
      position: static;
      transform: none;
      margin-top: 0.5rem;
      min-width: auto;
      width: 100%;
      padding: var(--spacing-md);
      display: none;
    }

    .nav-dropdown.active .dropdown-menu {
      display: block;
      opacity: 1;
      visibility: visible;
    }

    .dropdown-trigger {
      justify-content: space-between;
      width: 100%;
    }

    .dropdown-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }

    .mobile-menu-toggle {
      display: flex;
    }

    .btn-primary {
      display: none;
    }
  }

  @media (max-width: 480px) {
    .header {
      padding: 0 1rem;
    }

    .nav {
      height: 65px;
    }

    .logo {
      font-size: 1.125rem;
    }

    .nav-links {
      top: 65px;
      padding: 1.5rem 1rem;
    }
  }
</style>

<script>
  function initMobileMenu() {
    const toggle = document.querySelector('.mobile-menu-toggle');
    const menu = document.querySelector('.nav-links');

    if (toggle && menu) {
      toggle.addEventListener('click', () => {
        menu.classList.toggle('active');
        toggle.classList.toggle('active');
      });
    }

    // Mobile dropdown toggle
    const dropdownTriggers = document.querySelectorAll('.nav-dropdown .dropdown-trigger');
    dropdownTriggers.forEach(trigger => {
      trigger.addEventListener('click', (e) => {
        if (window.innerWidth <= 768) {
          e.preventDefault();
          const parent = trigger.closest('.nav-dropdown');
          parent?.classList.toggle('active');
        }
      });
    });
  }

  initMobileMenu();
  document.addEventListener('astro:page-load', initMobileMenu);

  // Load dynamic site name and logo from API
  async function loadSiteName() {
    try {
      const response = await fetch('/api/admin/content');
      if (response.ok) {
        const data = await response.json();
        if (data.success && data.content) {
          // Update site name
          if (data.content.site_name) {
            const siteNameEl = document.getElementById('header-site-name');
            if (siteNameEl) {
              siteNameEl.textContent = data.content.site_name;
            }
          }

          // Update logo if available
          if (data.content.site_logo && data.content.site_logo.trim() !== '') {
            const logoImg = document.getElementById('header-logo-img') as HTMLImageElement;
            const logoText = document.getElementById('header-site-name');

            if (logoImg) {
              logoImg.src = data.content.site_logo;
              logoImg.style.display = 'block';

              // Hide text logo when image logo is shown
              if (logoText) {
                logoText.style.display = 'none';
              }

              // Fallback to text if image fails to load
              logoImg.onerror = () => {
                logoImg.style.display = 'none';
                if (logoText) {
                  logoText.style.display = 'inline';
                }
              };
            }
          }
        }
      }
    } catch (error) {
      console.error('Error loading site name:', error);
    }
  }

  loadSiteName();
  document.addEventListener('astro:page-load', loadSiteName);
</script>
